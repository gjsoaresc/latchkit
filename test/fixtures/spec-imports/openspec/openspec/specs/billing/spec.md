# Billing Specification

Original fixture prose for Latchkit's test suite. This capability has a
recognizable `## Requirements` heading but no `### Requirement:` entries
under it yet, to exercise the `partial` current-spec classification.

## Purpose

Invoicing for the sample application used by this fixture.

## Requirements

<!-- Nothing has been written here yet. -->
