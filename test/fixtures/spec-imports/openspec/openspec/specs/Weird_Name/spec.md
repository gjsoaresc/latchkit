# Weird Name Specification

Original fixture prose for Latchkit's test suite. The containing directory
name uses an uppercase letter and an underscore, which does not match the
pinned kebab-case capability-path convention, even though the content below
is otherwise well-formed.

## Purpose

Exercises the `ambiguous` current-spec classification.

## Requirements

### Requirement: Placeholder Requirement

The system SHALL do something placeholder-shaped for this fixture.

#### Scenario: Placeholder scenario

- **WHEN** the placeholder condition holds
- **THEN** the placeholder outcome happens
