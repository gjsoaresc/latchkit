## Why

Original fixture prose for Latchkit's test suite. This change's directory
name uses an underscore instead of a hyphen, which does not match the
pinned kebab-case change-name convention, even though its content is
otherwise well-formed.

## What Changes

Exercises the `ambiguous` change classification.

## Capabilities

### New Capabilities

- `placeholder`: placeholder capability for this fixture

## Impact

None; this is a fixture-only change.
