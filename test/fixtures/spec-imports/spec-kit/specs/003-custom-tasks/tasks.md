Work tracker export (2026-01-05)

This team tracks work in a spreadsheet and exported it here as plain prose
instead of the pinned Spec Kit checklist format. Nothing below matches the
"Txxx" task identifier convention, so this fixture exercises the
"unsupported-version" discovery status.

1. Add the sign-in screen.
2. Wire up the reset flow.
