# Feature Specification: Not A Spec Kit Feature

**Status**: Draft

This file sits at the fixture root, outside `specs/`, on purpose. The
adapter only classifies files reached through `specs/<feature>/`, so a
generic root-level `spec.md` like this one must never be classified as a
Spec Kit feature by its filename alone.
