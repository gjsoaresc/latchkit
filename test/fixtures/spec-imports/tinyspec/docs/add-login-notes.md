# Add Login Notes

Original fixture prose for Latchkit's test suite. This is a plain
supporting document linked from the `add-login` TinySpec file, living
outside `.specs/` so it is never itself discovered as a spec entry.

The existing user table already stores a hashed password, so no schema
change is needed for the first version of sign-in.
