# Legacy Notes

Original fixture prose for Latchkit's test suite. This file sits at the
location and follows the filename convention a TinySpec file would use,
but it has no `tinySpec:` front matter at all — it must not be classified
as a supported TinySpec file just because of its filename and location.

Random notes: check with the design team about updated icons before the
next release.
