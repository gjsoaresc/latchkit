/* eslint-disable */
// prettier-ignore
// ----------------------------------------------------------------------------
//
//  Welcome to Baml! To use this generated code, please run the following:
//
//  $ pnpm add @boundaryml/baml-bridge
//  $ npm install @boundaryml/baml-bridge
//  $ yarn add @boundaryml/baml-bridge
//
// ----------------------------------------------------------------------------
// This file was generated by BAML: please do not edit it. Instead, edit the
// BAML files and re-generate this code using: baml-cli generate

import { defineFunction, type BamlCallContext } from "@boundaryml/baml-bridge";
import type * as ai from "../../../ai/index.js";
import type * as baml from "../../../baml/index.js";
import type * as vendor from "../../../vendor/index.js";

/**
 * Credential and region inputs for `sign_request` / `resolve_region`.
 * 
 * Each field mirrors a client option. Everything except `service` is optional;
 * unset fields fall through to the AWS provider chain.
 *
 * Attributes:
 *   region: Explicit region; `null` resolves from `AWS_REGION`, `AWS_DEFAULT_REGION`,
 *     or the active profile.
 *   profile: Named profile to read from the shared config/credentials files;
 *     `null` uses `AWS_PROFILE`, else `default`.
 *   access_key_id: Explicit access key. Only used together with `secret_access_key` — a
 *     lone key id falls through to the chain rather than signing with half an
 *     identity.
 *   secret_access_key
 *   session_token: Session token for temporary credentials (STS/SSO), sent as
 *     `x-amz-security-token`.
 *   service: The AWS service the signature is scoped to, e.g. `"bedrock"`.
 */
export class AwsSignOptions$stream {
  region!: string | null;
  profile!: string | null;
  access_key_id!: string | null;
  secret_access_key!: string | null;
  session_token!: string | null;
  service!: string | null;
  constructor(init: {
    region: string | null;
    profile: string | null;
    access_key_id: string | null;
    secret_access_key: string | null;
    session_token: string | null;
    service: string | null;
  }) {
    Object.assign(this, init);
  }
}

/**
 * Credential and region inputs for `sign_request` / `resolve_region`.
 * 
 * Each field mirrors a client option. Everything except `service` is optional;
 * unset fields fall through to the AWS provider chain.
 *
 * Attributes:
 *   region: Explicit region; `null` resolves from `AWS_REGION`, `AWS_DEFAULT_REGION`,
 *     or the active profile.
 *   profile: Named profile to read from the shared config/credentials files;
 *     `null` uses `AWS_PROFILE`, else `default`.
 *   access_key_id: Explicit access key. Only used together with `secret_access_key` — a
 *     lone key id falls through to the chain rather than signing with half an
 *     identity.
 *   secret_access_key
 *   session_token: Session token for temporary credentials (STS/SSO), sent as
 *     `x-amz-security-token`.
 *   service: The AWS service the signature is scoped to, e.g. `"bedrock"`.
 */
export class AwsSignOptions {
  region!: string | null;
  profile!: string | null;
  access_key_id!: string | null;
  secret_access_key!: string | null;
  session_token!: string | null;
  service!: string;
  constructor(init: {
    region: string | null;
    profile: string | null;
    access_key_id: string | null;
    secret_access_key: string | null;
    session_token: string | null;
    service: string;
  }) {
    Object.assign(this, init);
  }
}

/**
 * SigV4-signs `req` and returns it with the signature headers applied
 * (`authorization`, `x-amz-date`, and `x-amz-security-token` for session
 * credentials). Raises if no credentials or no region can be resolved.
 * 
 * The signature covers the request's method, URL, headers, and body, so this
 * MUST be the last mutation of the request — a header or body change made
 * afterwards invalidates it and the call is rejected with a 403.
 * 
 * Credentials come from `opts.access_key_id` + `opts.secret_access_key` (+
 * optional `opts.session_token`) when both halves are present, else from the
 * AWS provider chain: environment, the shared config/credentials files (static
 * keys, `credential_process`, SSO cache), the container endpoint, then EC2
 * IMDS.
 * @throws AccessError
 * @throws Io
 */
export const sign_request = defineFunction("aws.internal.sign_request", "sync", ["req", "opts"]) as (req: baml.http.Request, opts: AwsSignOptions, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => baml.http.Request;

/**
 * SigV4-signs `req` and returns it with the signature headers applied
 * (`authorization`, `x-amz-date`, and `x-amz-security-token` for session
 * credentials). Raises if no credentials or no region can be resolved.
 * 
 * The signature covers the request's method, URL, headers, and body, so this
 * MUST be the last mutation of the request — a header or body change made
 * afterwards invalidates it and the call is rejected with a 403.
 * 
 * Credentials come from `opts.access_key_id` + `opts.secret_access_key` (+
 * optional `opts.session_token`) when both halves are present, else from the
 * AWS provider chain: environment, the shared config/credentials files (static
 * keys, `credential_process`, SSO cache), the container endpoint, then EC2
 * IMDS.
 * @throws AccessError
 * @throws Io
 */
export const sign_request_async = defineFunction("aws.internal.sign_request", "async", ["req", "opts"]) as (req: baml.http.Request, opts: AwsSignOptions, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<baml.http.Request>;

/**
 * Resolves the AWS region: `region` when set, else `AWS_REGION`,
 * `AWS_DEFAULT_REGION`, or the active profile's `region` key. `null` means
 * "not discoverable" — the caller decides how to report it (endpoint
 * construction usually needs it before signing does).
 * @throws AccessError
 * @throws Io
 */
export const resolve_region = defineFunction("aws.internal.resolve_region", "sync", ["region", "profile"]) as (region: string | null, profile: string | null, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => string | null;

/**
 * Resolves the AWS region: `region` when set, else `AWS_REGION`,
 * `AWS_DEFAULT_REGION`, or the active profile's `region` key. `null` means
 * "not discoverable" — the caller decides how to report it (endpoint
 * construction usually needs it before signing does).
 * @throws AccessError
 * @throws Io
 */
export const resolve_region_async = defineFunction("aws.internal.resolve_region", "async", ["region", "profile"]) as (region: string | null, profile: string | null, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<string | null>;

/**
 * `AudioSource` is a `bytes` | `s3Location` union exactly like the other three
 * media sources (API_runtime_AudioSource), so it reuses `MediaSource`. An
 * earlier revision modeled bytes only and hard-rejected `s3://` audio; that
 * was wrong.
 */
export class AudioBlock$stream {
  format!: string | null;
  source!: MediaSource$stream | null;
  constructor(init: {
    format: string | null;
    source: MediaSource$stream | null;
  }) {
    Object.assign(this, init);
  }
}

export class BedrockPrompt$stream {
  system!: SystemContentBlock$stream[];
  messages!: Message$stream[];
  constructor(init: {
    system: SystemContentBlock$stream[];
    messages: Message$stream[];
  }) {
    Object.assign(this, init);
  }
}

/**
 * A prompt-caching checkpoint. Everything *before* the checkpoint in the
 * request is eligible for reuse on the next call. `"default"` is the only
 * documented type.
 */
export class CachePointBlock$stream {
  type!: string | null;
  constructor(init: {
    type: string | null;
  }) {
    Object.assign(this, init);
  }
}

/**
 * The `ContentBlock` union of a Converse message. Exactly one member is set;
 * build with the static constructors rather than the literal.
 */
export class ContentBlock$stream {
  text!: string | null;
  image!: ImageBlock$stream | null;
  video!: VideoBlock$stream | null;
  document!: DocumentBlock$stream | null;
  audio!: AudioBlock$stream | null;
  toolUse!: ToolUseBlock$stream | null;
  toolResult!: ToolResultBlock$stream | null;
  cachePoint!: CachePointBlock$stream | null;
  constructor(init: {
    text: string | null;
    image: ImageBlock$stream | null;
    video: VideoBlock$stream | null;
    document: DocumentBlock$stream | null;
    audio: AudioBlock$stream | null;
    toolUse: ToolUseBlock$stream | null;
    toolResult: ToolResultBlock$stream | null;
    cachePoint: CachePointBlock$stream | null;
  }) {
    Object.assign(this, init);
  }
}

export class ConverseMetrics$stream {
  latencyMs!: number | null;
  constructor(init: {
    latencyMs: number | null;
  }) {
    Object.assign(this, init);
  }
}

export class ConverseOutput$stream {
  message!: ResponseMessage$stream | null;
  constructor(init: {
    message: ResponseMessage$stream | null;
  }) {
    Object.assign(this, init);
  }
}

/**
 * The Converse request body. The model id and the credentials are NOT part of
 * it — they live in the URL path and the signed headers respectively
 * (asserted by `bedrock_no_model_or_creds_in_body`,
 * `build_request/bedrock.rs:671-677`).
 *
 * Attributes:
 *   messages
 *   system
 *   inferenceConfig
 *   toolConfig
 *   additionalModelRequestFields: Model-specific parameters Converse has no first-class field for
 *     (`top_k`, `anthropic_beta`, …). Open-ended by definition — the second
 *     `baml.json.json` escape hatch.
 */
export class ConverseRequest$stream {
  messages!: Message$stream[];
  system!: SystemContentBlock$stream[] | null;
  inferenceConfig!: InferenceConfig$stream | null;
  toolConfig!: ToolConfig$stream | null;
  additionalModelRequestFields!: baml.json.json$stream | null;
  constructor(init: {
    messages: Message$stream[];
    system: SystemContentBlock$stream[] | null;
    inferenceConfig: InferenceConfig$stream | null;
    toolConfig: ToolConfig$stream | null;
    additionalModelRequestFields: baml.json.json$stream | null;
  }) {
    Object.assign(this, init);
  }
}

export class ConverseResponse$stream {
  output!: ConverseOutput$stream | null;
  stopReason!: string | null;
  usage!: ConverseUsage$stream | null;
  metrics!: ConverseMetrics$stream | null;
  additionalModelResponseFields!: baml.json.json$stream | null;
  constructor(init: {
    output: ConverseOutput$stream | null;
    stopReason: string | null;
    usage: ConverseUsage$stream | null;
    metrics: ConverseMetrics$stream | null;
    additionalModelResponseFields: baml.json.json$stream | null;
  }) {
    Object.assign(this, init);
  }
}

/**
 * Converse token accounting. `cacheReadInputTokens` / `cacheWriteInputTokens`
 * are reported for prompt-cached requests — sys_llm hardcoded
 * `cached_input_tokens: None` claiming Converse never reports them
 * (`parse_response/bedrock.rs:124`), which was simply wrong; the engine read
 * them (`aws_client.rs:1481-1484`). Fixed here.
 */
export class ConverseUsage$stream {
  inputTokens!: number | null;
  outputTokens!: number | null;
  totalTokens!: number | null;
  cacheReadInputTokens!: number | null;
  cacheWriteInputTokens!: number | null;
  constructor(init: {
    inputTokens: number | null;
    outputTokens: number | null;
    totalTokens: number | null;
    cacheReadInputTokens: number | null;
    cacheWriteInputTokens: number | null;
  }) {
    Object.assign(this, init);
  }
}

export class DocumentBlock$stream {
  format!: string | null;
  name!: string | null;
  source!: MediaSource$stream | null;
  constructor(init: {
    format: string | null;
    name: string | null;
    source: MediaSource$stream | null;
  }) {
    Object.assign(this, init);
  }
}

export class ImageBlock$stream {
  format!: string | null;
  source!: MediaSource$stream | null;
  constructor(init: {
    format: string | null;
    source: MediaSource$stream | null;
  }) {
    Object.assign(this, init);
  }
}

/**
 * `inferenceConfig`. Omitted from the body entirely when every member is
 * unset (`forks/aws-bedrock/src/lib.rs:249-256`).
 */
export class InferenceConfig$stream {
  maxTokens!: number | null;
  temperature!: number | null;
  topP!: number | null;
  stopSequences!: string[] | null;
  constructor(init: {
    maxTokens: number | null;
    temperature: number | null;
    topP: number | null;
    stopSequences: string[] | null;
  }) {
    Object.assign(this, init);
  }
}

/**
 * `ImageSource` / `VideoSource` / `DocumentSource` / `AudioSource` union:
 * base64 `bytes` or an `s3Location`. All four media unions have the same two
 * members in the current Converse API.
 */
export class MediaSource$stream {
  bytes!: string | null;
  s3Location!: S3Location$stream | null;
  constructor(init: {
    bytes: string | null;
    s3Location: S3Location$stream | null;
  }) {
    Object.assign(this, init);
  }
}

export class Message$stream {
  role!: string | null;
  content!: ContentBlock$stream[];
  constructor(init: {
    role: string | null;
    content: ContentBlock$stream[];
  }) {
    Object.assign(this, init);
  }
}

export class PendingMessage$stream {
  message!: Message$stream | null;
  tool_results!: boolean | null;
  constructor(init: {
    message: Message$stream | null;
    tool_results: boolean | null;
  }) {
    Object.assign(this, init);
  }
}

/**
 * Attributes:
 *   reasoningText
 *   redactedContent: Present instead of `reasoningText` when the provider redacted the
 *     thinking; opaque bytes with nothing readable to surface.
 */
export class ReasoningContent$stream {
  reasoningText!: ReasoningText$stream | null;
  redactedContent!: string | null;
  constructor(init: {
    reasoningText: ReasoningText$stream | null;
    redactedContent: string | null;
  }) {
    Object.assign(this, init);
  }
}

export class ReasoningText$stream {
  text!: string | null;
  signature!: string | null;
  constructor(init: {
    text: string | null;
    signature: string | null;
  }) {
    Object.assign(this, init);
  }
}

export class ResponseContentBlock$stream {
  text!: string | null;
  toolUse!: ResponseToolUse$stream | null;
  reasoningContent!: ReasoningContent$stream | null;
  constructor(init: {
    text: string | null;
    toolUse: ResponseToolUse$stream | null;
    reasoningContent: ReasoningContent$stream | null;
  }) {
    Object.assign(this, init);
  }
}

export class ResponseMessage$stream {
  role!: string | null;
  content!: ResponseContentBlock$stream[] | null;
  constructor(init: {
    role: string | null;
    content: ResponseContentBlock$stream[] | null;
  }) {
    Object.assign(this, init);
  }
}

export class ResponseToolUse$stream {
  toolUseId!: string | null;
  name!: string | null;
  input!: { [key: string]: unknown } | null;
  constructor(init: {
    toolUseId: string | null;
    name: string | null;
    input: { [key: string]: unknown } | null;
  }) {
    Object.assign(this, init);
  }
}

/**
 * An object in an S3 bucket, for media the account already stores there.
 */
export class S3Location$stream {
  uri!: string | null;
  bucketOwner!: string | null;
  constructor(init: {
    uri: string | null;
    bucketOwner: string | null;
  }) {
    Object.assign(this, init);
  }
}

/**
 * A specific tool the model must call.
 */
export class SpecificToolChoice$stream {
  name!: string | null;
  constructor(init: {
    name: string | null;
  }) {
    Object.assign(this, init);
  }
}

/**
 * The `SystemContentBlock` union. Converse system content is text (or a cache
 * checkpoint) only — media in a system message is rejected, matching
 * `build_request/bedrock.rs:174-176`.
 */
export class SystemContentBlock$stream {
  text!: string | null;
  cachePoint!: CachePointBlock$stream | null;
  constructor(init: {
    text: string | null;
    cachePoint: CachePointBlock$stream | null;
  }) {
    Object.assign(this, init);
  }
}

/**
 * The `ToolChoice` union: `{"auto":{}}`, `{"any":{}}`, or
 * `{"tool":{"name":"…"}}`.
 */
export class ToolChoice$stream {
  auto!: { [key: string]: unknown } | null;
  any!: { [key: string]: unknown } | null;
  tool!: SpecificToolChoice$stream | null;
  constructor(init: {
    auto: { [key: string]: unknown } | null;
    any: { [key: string]: unknown } | null;
    tool: SpecificToolChoice$stream | null;
  }) {
    Object.assign(this, init);
  }
}

export class ToolConfig$stream {
  tools!: ToolEntry$stream[];
  toolChoice!: ToolChoice$stream | null;
  constructor(init: {
    tools: ToolEntry$stream[];
    toolChoice: ToolChoice$stream | null;
  }) {
    Object.assign(this, init);
  }
}

/**
 * The `Tool` union: a `toolSpec`, or a `cachePoint` terminating the cacheable
 * tool prefix.
 */
export class ToolEntry$stream {
  toolSpec!: ToolSpec$stream | null;
  cachePoint!: CachePointBlock$stream | null;
  constructor(init: {
    toolSpec: ToolSpec$stream | null;
    cachePoint: CachePointBlock$stream | null;
  }) {
    Object.assign(this, init);
  }
}

/**
 * A tool's parameter schema. The schema is genuinely open-ended (arbitrary
 * JSON Schema authored by the tool), so it is one of the two `baml.json.json`
 * escape hatches in this file — see `_prune_nulls`.
 * 
 * The Converse wire key is `json`, but that name collides with
 * `pydantic.BaseModel.json` in generated Python SDKs, so the field is
 * `schema` with an explicit wire mapping (the same reserved-word pattern as
 * the chat client's `fn` ↔ `"function"`).
 */
export class ToolInputSchema$stream {
  json_schema!: baml.json.json$stream | null;
  constructor(init: {
    json_schema: baml.json.json$stream | null;
  }) {
    Object.assign(this, init);
  }
}

export class ToolResultBlock$stream {
  toolUseId!: string | null;
  content!: ToolResultContentBlock$stream[];
  status!: string | null;
  constructor(init: {
    toolUseId: string | null;
    content: ToolResultContentBlock$stream[];
    status: string | null;
  }) {
    Object.assign(this, init);
  }
}

/**
 * One piece of a tool's result. Only `text` is modeled: `ai.events` carries
 * tool output as a string (`ToolCompleted.output` / `ToolFailed.message`), so
 * the `json`/`image`/`document` members of the wire union have no source.
 */
export class ToolResultContentBlock$stream {
  text!: string | null;
  constructor(init: {
    text: string | null;
  }) {
    Object.assign(this, init);
  }
}

export class ToolSpec$stream {
  name!: string | null;
  description!: string | null;
  inputSchema!: ToolInputSchema$stream | null;
  constructor(init: {
    name: string | null;
    description: string | null;
    inputSchema: ToolInputSchema$stream | null;
  }) {
    Object.assign(this, init);
  }
}

export class ToolUseBlock$stream {
  toolUseId!: string | null;
  name!: string | null;
  input!: { [key: string]: unknown };
  constructor(init: {
    toolUseId: string | null;
    name: string | null;
    input: { [key: string]: unknown };
  }) {
    Object.assign(this, init);
  }
}

export class VideoBlock$stream {
  format!: string | null;
  source!: MediaSource$stream | null;
  constructor(init: {
    format: string | null;
    source: MediaSource$stream | null;
  }) {
    Object.assign(this, init);
  }
}

export const _is_unreserved_byte = defineFunction("aws.internal._is_unreserved_byte", "sync", ["byte"]) as (byte: number, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => boolean;

export const _is_unreserved_byte_async = defineFunction("aws.internal._is_unreserved_byte", "async", ["byte"]) as (byte: number, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<boolean>;

/**
 * Percent-encode `value` as a single URI path label: `/` becomes `%2F` and
 * `:` becomes `%3A`, so a Bedrock ARN or inference-profile id survives as one
 * path segment. Also used for query-parameter keys and values, whose SigV4
 * canonical form re-encodes with the same unreserved set.
 * @throws InvalidArgument
 */
export const encode_path_label = defineFunction("aws.internal.encode_path_label", "sync", ["value"]) as (value: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => string;

/**
 * Percent-encode `value` as a single URI path label: `/` becomes `%2F` and
 * `:` becomes `%3A`, so a Bedrock ARN or inference-profile id survives as one
 * path segment. Also used for query-parameter keys and values, whose SigV4
 * canonical form re-encodes with the same unreserved set.
 * @throws InvalidArgument
 */
export const encode_path_label_async = defineFunction("aws.internal.encode_path_label", "async", ["value"]) as (value: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<string>;

/**
 * The Converse request path for a model id, inference-profile id, or ARN.
 * @throws InvalidArgument
 */
export const converse_model_path = defineFunction("aws.internal.converse_model_path", "sync", ["model_id"]) as (model_id: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => string;

/**
 * The Converse request path for a model id, inference-profile id, or ARN.
 * @throws InvalidArgument
 */
export const converse_model_path_async = defineFunction("aws.internal.converse_model_path", "async", ["model_id"]) as (model_id: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<string>;

/**
 * An object in an S3 bucket, for media the account already stores there.
 */
export class S3Location {
  uri!: string;
  bucketOwner!: string | null;
  constructor(init: {
    uri: string;
    bucketOwner: string | null;
  }) {
    Object.assign(this, init);
  }
}

/**
 * `ImageSource` / `VideoSource` / `DocumentSource` / `AudioSource` union:
 * base64 `bytes` or an `s3Location`. All four media unions have the same two
 * members in the current Converse API.
 */
export class MediaSource {
  bytes!: string | null;
  s3Location!: S3Location | null;
  constructor(init: {
    bytes: string | null;
    s3Location: S3Location | null;
  }) {
    Object.assign(this, init);
  }
}

export class ImageBlock {
  format!: string;
  source!: MediaSource;
  constructor(init: {
    format: string;
    source: MediaSource;
  }) {
    Object.assign(this, init);
  }
}

export class VideoBlock {
  format!: string;
  source!: MediaSource;
  constructor(init: {
    format: string;
    source: MediaSource;
  }) {
    Object.assign(this, init);
  }
}

export class DocumentBlock {
  format!: string;
  name!: string;
  source!: MediaSource;
  constructor(init: {
    format: string;
    name: string;
    source: MediaSource;
  }) {
    Object.assign(this, init);
  }
}

/**
 * `AudioSource` is a `bytes` | `s3Location` union exactly like the other three
 * media sources (API_runtime_AudioSource), so it reuses `MediaSource`. An
 * earlier revision modeled bytes only and hard-rejected `s3://` audio; that
 * was wrong.
 */
export class AudioBlock {
  format!: string;
  source!: MediaSource;
  constructor(init: {
    format: string;
    source: MediaSource;
  }) {
    Object.assign(this, init);
  }
}

/**
 * A prompt-caching checkpoint. Everything *before* the checkpoint in the
 * request is eligible for reuse on the next call. `"default"` is the only
 * documented type.
 */
export class CachePointBlock {
  type!: string;
  constructor(init: {
    type: string;
  }) {
    Object.assign(this, init);
  }
}

export class ToolUseBlock {
  toolUseId!: string;
  name!: string;
  input!: { [key: string]: unknown };
  constructor(init: {
    toolUseId: string;
    name: string;
    input: { [key: string]: unknown };
  }) {
    Object.assign(this, init);
  }
}

/**
 * One piece of a tool's result. Only `text` is modeled: `ai.events` carries
 * tool output as a string (`ToolCompleted.output` / `ToolFailed.message`), so
 * the `json`/`image`/`document` members of the wire union have no source.
 */
export class ToolResultContentBlock {
  text!: string;
  constructor(init: {
    text: string;
  }) {
    Object.assign(this, init);
  }
}

export class ToolResultBlock {
  toolUseId!: string;
  content!: ToolResultContentBlock[];
  status!: string | null;
  constructor(init: {
    toolUseId: string;
    content: ToolResultContentBlock[];
    status: string | null;
  }) {
    Object.assign(this, init);
  }
}

/**
 * The `ContentBlock` union of a Converse message. Exactly one member is set;
 * build with the static constructors rather than the literal.
 */
export class ContentBlock {
  text!: string | null;
  image!: ImageBlock | null;
  video!: VideoBlock | null;
  document!: DocumentBlock | null;
  audio!: AudioBlock | null;
  toolUse!: ToolUseBlock | null;
  toolResult!: ToolResultBlock | null;
  cachePoint!: CachePointBlock | null;
  constructor(init: {
    text: string | null;
    image: ImageBlock | null;
    video: VideoBlock | null;
    document: DocumentBlock | null;
    audio: AudioBlock | null;
    toolUse: ToolUseBlock | null;
    toolResult: ToolResultBlock | null;
    cachePoint: CachePointBlock | null;
  }) {
    Object.assign(this, init);
  }
  static of_text = defineFunction("aws.internal.ContentBlock.of_text", "sync", ["text"]) as (text: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => ContentBlock;
  static of_text_async = defineFunction("aws.internal.ContentBlock.of_text", "async", ["text"]) as (text: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<ContentBlock>;
  static of_image = defineFunction("aws.internal.ContentBlock.of_image", "sync", ["image"]) as (image: ImageBlock, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => ContentBlock;
  static of_image_async = defineFunction("aws.internal.ContentBlock.of_image", "async", ["image"]) as (image: ImageBlock, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<ContentBlock>;
  static of_video = defineFunction("aws.internal.ContentBlock.of_video", "sync", ["video"]) as (video: VideoBlock, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => ContentBlock;
  static of_video_async = defineFunction("aws.internal.ContentBlock.of_video", "async", ["video"]) as (video: VideoBlock, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<ContentBlock>;
  static of_document = defineFunction("aws.internal.ContentBlock.of_document", "sync", ["document"]) as (document: DocumentBlock, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => ContentBlock;
  static of_document_async = defineFunction("aws.internal.ContentBlock.of_document", "async", ["document"]) as (document: DocumentBlock, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<ContentBlock>;
  static of_audio = defineFunction("aws.internal.ContentBlock.of_audio", "sync", ["audio"]) as (audio: AudioBlock, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => ContentBlock;
  static of_audio_async = defineFunction("aws.internal.ContentBlock.of_audio", "async", ["audio"]) as (audio: AudioBlock, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<ContentBlock>;
  static of_tool_use = defineFunction("aws.internal.ContentBlock.of_tool_use", "sync", ["tool_use"]) as (tool_use: ToolUseBlock, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => ContentBlock;
  static of_tool_use_async = defineFunction("aws.internal.ContentBlock.of_tool_use", "async", ["tool_use"]) as (tool_use: ToolUseBlock, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<ContentBlock>;
  static of_tool_result = defineFunction("aws.internal.ContentBlock.of_tool_result", "sync", ["tool_result"]) as (tool_result: ToolResultBlock, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => ContentBlock;
  static of_tool_result_async = defineFunction("aws.internal.ContentBlock.of_tool_result", "async", ["tool_result"]) as (tool_result: ToolResultBlock, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<ContentBlock>;
  static of_cache_point = defineFunction("aws.internal.ContentBlock.of_cache_point", "sync", []) as ($opts?: { $ctx?: BamlCallContext | undefined } | undefined) => ContentBlock;
  static of_cache_point_async = defineFunction("aws.internal.ContentBlock.of_cache_point", "async", []) as ($opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<ContentBlock>;
}

export const _empty_content_block = defineFunction("aws.internal._empty_content_block", "sync", []) as ($opts?: { $ctx?: BamlCallContext | undefined } | undefined) => ContentBlock;

export const _empty_content_block_async = defineFunction("aws.internal._empty_content_block", "async", []) as ($opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<ContentBlock>;

/**
 * The `SystemContentBlock` union. Converse system content is text (or a cache
 * checkpoint) only — media in a system message is rejected, matching
 * `build_request/bedrock.rs:174-176`.
 */
export class SystemContentBlock {
  text!: string | null;
  cachePoint!: CachePointBlock | null;
  constructor(init: {
    text: string | null;
    cachePoint: CachePointBlock | null;
  }) {
    Object.assign(this, init);
  }
  static of_text = defineFunction("aws.internal.SystemContentBlock.of_text", "sync", ["text"]) as (text: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => SystemContentBlock;
  static of_text_async = defineFunction("aws.internal.SystemContentBlock.of_text", "async", ["text"]) as (text: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<SystemContentBlock>;
  static of_cache_point = defineFunction("aws.internal.SystemContentBlock.of_cache_point", "sync", []) as ($opts?: { $ctx?: BamlCallContext | undefined } | undefined) => SystemContentBlock;
  static of_cache_point_async = defineFunction("aws.internal.SystemContentBlock.of_cache_point", "async", []) as ($opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<SystemContentBlock>;
}

export class Message {
  role!: string;
  content!: ContentBlock[];
  constructor(init: {
    role: string;
    content: ContentBlock[];
  }) {
    Object.assign(this, init);
  }
}

/**
 * `inferenceConfig`. Omitted from the body entirely when every member is
 * unset (`forks/aws-bedrock/src/lib.rs:249-256`).
 */
export class InferenceConfig {
  maxTokens!: number | null;
  temperature!: number | null;
  topP!: number | null;
  stopSequences!: string[] | null;
  constructor(init: {
    maxTokens: number | null;
    temperature: number | null;
    topP: number | null;
    stopSequences: string[] | null;
  }) {
    Object.assign(this, init);
  }
}

/**
 * A tool's parameter schema. The schema is genuinely open-ended (arbitrary
 * JSON Schema authored by the tool), so it is one of the two `baml.json.json`
 * escape hatches in this file — see `_prune_nulls`.
 * 
 * The Converse wire key is `json`, but that name collides with
 * `pydantic.BaseModel.json` in generated Python SDKs, so the field is
 * `schema` with an explicit wire mapping (the same reserved-word pattern as
 * the chat client's `fn` ↔ `"function"`).
 */
export class ToolInputSchema {
  json_schema!: baml.json.json;
  constructor(init: {
    json_schema: baml.json.json;
  }) {
    Object.assign(this, init);
  }
}

export class ToolSpec {
  name!: string;
  description!: string | null;
  inputSchema!: ToolInputSchema;
  constructor(init: {
    name: string;
    description: string | null;
    inputSchema: ToolInputSchema;
  }) {
    Object.assign(this, init);
  }
}

/**
 * The `Tool` union: a `toolSpec`, or a `cachePoint` terminating the cacheable
 * tool prefix.
 */
export class ToolEntry {
  toolSpec!: ToolSpec | null;
  cachePoint!: CachePointBlock | null;
  constructor(init: {
    toolSpec: ToolSpec | null;
    cachePoint: CachePointBlock | null;
  }) {
    Object.assign(this, init);
  }
  static of_spec = defineFunction("aws.internal.ToolEntry.of_spec", "sync", ["spec"]) as (spec: ToolSpec, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => ToolEntry;
  static of_spec_async = defineFunction("aws.internal.ToolEntry.of_spec", "async", ["spec"]) as (spec: ToolSpec, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<ToolEntry>;
  static of_cache_point = defineFunction("aws.internal.ToolEntry.of_cache_point", "sync", []) as ($opts?: { $ctx?: BamlCallContext | undefined } | undefined) => ToolEntry;
  static of_cache_point_async = defineFunction("aws.internal.ToolEntry.of_cache_point", "async", []) as ($opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<ToolEntry>;
}

/**
 * A specific tool the model must call.
 */
export class SpecificToolChoice {
  name!: string;
  constructor(init: {
    name: string;
  }) {
    Object.assign(this, init);
  }
}

/**
 * The `ToolChoice` union: `{"auto":{}}`, `{"any":{}}`, or
 * `{"tool":{"name":"…"}}`.
 */
export class ToolChoice {
  auto!: { [key: string]: unknown } | null;
  any!: { [key: string]: unknown } | null;
  tool!: SpecificToolChoice | null;
  constructor(init: {
    auto: { [key: string]: unknown } | null;
    any: { [key: string]: unknown } | null;
    tool: SpecificToolChoice | null;
  }) {
    Object.assign(this, init);
  }
}

export class ToolConfig {
  tools!: ToolEntry[];
  toolChoice!: ToolChoice | null;
  constructor(init: {
    tools: ToolEntry[];
    toolChoice: ToolChoice | null;
  }) {
    Object.assign(this, init);
  }
}

/**
 * The Converse request body. The model id and the credentials are NOT part of
 * it — they live in the URL path and the signed headers respectively
 * (asserted by `bedrock_no_model_or_creds_in_body`,
 * `build_request/bedrock.rs:671-677`).
 *
 * Attributes:
 *   messages
 *   system
 *   inferenceConfig
 *   toolConfig
 *   additionalModelRequestFields: Model-specific parameters Converse has no first-class field for
 *     (`top_k`, `anthropic_beta`, …). Open-ended by definition — the second
 *     `baml.json.json` escape hatch.
 */
export class ConverseRequest {
  messages!: Message[];
  system!: SystemContentBlock[] | null;
  inferenceConfig!: InferenceConfig | null;
  toolConfig!: ToolConfig | null;
  additionalModelRequestFields!: baml.json.json | null;
  constructor(init: {
    messages: Message[];
    system: SystemContentBlock[] | null;
    inferenceConfig: InferenceConfig | null;
    toolConfig: ToolConfig | null;
    additionalModelRequestFields: baml.json.json | null;
  }) {
    Object.assign(this, init);
  }
}

export const _is_opaque_key = defineFunction("aws.internal._is_opaque_key", "sync", ["key"]) as (key: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => boolean;

export const _is_opaque_key_async = defineFunction("aws.internal._is_opaque_key", "async", ["key"]) as (key: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<boolean>;

export const _prune_nulls = defineFunction("aws.internal._prune_nulls", "sync", ["value"]) as (value: baml.json.json, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => baml.json.json;

export const _prune_nulls_async = defineFunction("aws.internal._prune_nulls", "async", ["value"]) as (value: baml.json.json, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<baml.json.json>;

export const _reject = defineFunction("aws.internal._reject", "sync", ["detail"]) as (detail: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => ai.errors.InvalidRequest;

export const _reject_async = defineFunction("aws.internal._reject", "async", ["detail"]) as (detail: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<ai.errors.InvalidRequest>;

export const _is_s3_url = defineFunction("aws.internal._is_s3_url", "sync", ["url"]) as (url: string | null, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => boolean;

export const _is_s3_url_async = defineFunction("aws.internal._is_s3_url", "async", ["url"]) as (url: string | null, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<boolean>;

/**
 * @throws InvalidRequest
 */
export const _image_format = defineFunction("aws.internal._image_format", "sync", ["mime"]) as (mime: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => string;

/**
 * @throws InvalidRequest
 */
export const _image_format_async = defineFunction("aws.internal._image_format", "async", ["mime"]) as (mime: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<string>;

/**
 * @throws InvalidRequest
 */
export const _video_format = defineFunction("aws.internal._video_format", "sync", ["mime"]) as (mime: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => string;

/**
 * @throws InvalidRequest
 */
export const _video_format_async = defineFunction("aws.internal._video_format", "async", ["mime"]) as (mime: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<string>;

/**
 * @throws InvalidRequest
 */
export const _audio_format = defineFunction("aws.internal._audio_format", "sync", ["mime"]) as (mime: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => string;

/**
 * @throws InvalidRequest
 */
export const _audio_format_async = defineFunction("aws.internal._audio_format", "async", ["mime"]) as (mime: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<string>;

/**
 * @throws InvalidRequest
 */
export const _media_source = defineFunction("aws.internal._media_source", "sync", ["resolved", "kind"]) as (resolved: ai.wire.ResolvedMedia, kind: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => MediaSource;

/**
 * @throws InvalidRequest
 */
export const _media_source_async = defineFunction("aws.internal._media_source", "async", ["resolved", "kind"]) as (resolved: ai.wire.ResolvedMedia, kind: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<MediaSource>;

/**
 * @throws InvalidRequest
 * @throws NetworkFailure
 * @throws InvalidArgument
 * @throws Io
 */
export const _image_block = defineFunction("aws.internal._image_block", "sync", ["image"]) as (image: baml.media.Image, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => ContentBlock;

/**
 * @throws InvalidRequest
 * @throws NetworkFailure
 * @throws InvalidArgument
 * @throws Io
 */
export const _image_block_async = defineFunction("aws.internal._image_block", "async", ["image"]) as (image: baml.media.Image, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<ContentBlock>;

/**
 * @throws InvalidRequest
 * @throws NetworkFailure
 * @throws InvalidArgument
 * @throws Io
 */
export const _video_block = defineFunction("aws.internal._video_block", "sync", ["video"]) as (video: baml.media.Video, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => ContentBlock;

/**
 * @throws InvalidRequest
 * @throws NetworkFailure
 * @throws InvalidArgument
 * @throws Io
 */
export const _video_block_async = defineFunction("aws.internal._video_block", "async", ["video"]) as (video: baml.media.Video, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<ContentBlock>;

/**
 * @throws InvalidRequest
 * @throws NetworkFailure
 * @throws InvalidArgument
 * @throws Io
 */
export const _document_block = defineFunction("aws.internal._document_block", "sync", ["pdf"]) as (pdf: baml.media.Pdf, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => ContentBlock;

/**
 * @throws InvalidRequest
 * @throws NetworkFailure
 * @throws InvalidArgument
 * @throws Io
 */
export const _document_block_async = defineFunction("aws.internal._document_block", "async", ["pdf"]) as (pdf: baml.media.Pdf, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<ContentBlock>;

/**
 * @throws InvalidRequest
 * @throws NetworkFailure
 * @throws InvalidArgument
 * @throws Io
 */
export const _audio_block = defineFunction("aws.internal._audio_block", "sync", ["audio"]) as (audio: baml.media.Audio, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => ContentBlock;

/**
 * @throws InvalidRequest
 * @throws NetworkFailure
 * @throws InvalidArgument
 * @throws Io
 */
export const _audio_block_async = defineFunction("aws.internal._audio_block", "async", ["audio"]) as (audio: baml.media.Audio, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<ContentBlock>;

export class BedrockPrompt {
  system!: SystemContentBlock[];
  messages!: Message[];
  constructor(init: {
    system: SystemContentBlock[];
    messages: Message[];
  }) {
    Object.assign(this, init);
  }
}

/**
 * @throws InvalidRequest
 * @throws NetworkFailure
 * @throws InvalidArgument
 * @throws Io
 */
export const _prompt_content = defineFunction("aws.internal._prompt_content", "sync", ["message"]) as (message: ai.PromptMessage, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => ContentBlock[];

/**
 * @throws InvalidRequest
 * @throws NetworkFailure
 * @throws InvalidArgument
 * @throws Io
 */
export const _prompt_content_async = defineFunction("aws.internal._prompt_content", "async", ["message"]) as (message: ai.PromptMessage, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<ContentBlock[]>;

/**
 * @throws InvalidRequest
 */
export const _prompt_system = defineFunction("aws.internal._prompt_system", "sync", ["message"]) as (message: ai.PromptMessage, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => SystemContentBlock[];

/**
 * @throws InvalidRequest
 */
export const _prompt_system_async = defineFunction("aws.internal._prompt_system", "async", ["message"]) as (message: ai.PromptMessage, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<SystemContentBlock[]>;

/**
 * The per-message metadata key that requests a Bedrock prompt-caching
 * checkpoint after the message's content.
 * 
 * The engine emits `{"cachePoint":{"type":"default"}}` whenever a message
 * carries `cache_control` metadata
 * (`engine/.../aws_client.rs:123-155`). BAML's neutral spelling of that is a
 * `ai.PromptMessage.metadata` entry, and Bedrock's own name for the feature
 * is a cache *point*, so the key is `cache_point` — any non-null value
 * (`true` is the idiomatic one) turns the checkpoint on.
 * 
 * NOTE: `ai.internal.make_role` currently builds every role marker with empty
 * metadata, so nothing populates this yet from the prompt DSL. Until that
 * lands, use the client's `cache_system_prompt` / `cache_tools` options,
 * which do not depend on per-message metadata.
 */
export const _wants_cache_point = defineFunction("aws.internal._wants_cache_point", "sync", ["message"]) as (message: ai.PromptMessage, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => boolean;

/**
 * The per-message metadata key that requests a Bedrock prompt-caching
 * checkpoint after the message's content.
 * 
 * The engine emits `{"cachePoint":{"type":"default"}}` whenever a message
 * carries `cache_control` metadata
 * (`engine/.../aws_client.rs:123-155`). BAML's neutral spelling of that is a
 * `ai.PromptMessage.metadata` entry, and Bedrock's own name for the feature
 * is a cache *point*, so the key is `cache_point` — any non-null value
 * (`true` is the idiomatic one) turns the checkpoint on.
 * 
 * NOTE: `ai.internal.make_role` currently builds every role marker with empty
 * metadata, so nothing populates this yet from the prompt DSL. Until that
 * lands, use the client's `cache_system_prompt` / `cache_tools` options,
 * which do not depend on per-message metadata.
 */
export const _wants_cache_point_async = defineFunction("aws.internal._wants_cache_point", "async", ["message"]) as (message: ai.PromptMessage, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<boolean>;

export const _conversation_role = defineFunction("aws.internal._conversation_role", "sync", ["role"]) as (role: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => string;

export const _conversation_role_async = defineFunction("aws.internal._conversation_role", "async", ["role"]) as (role: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<string>;

/**
 * @throws InvalidRequest
 * @throws NetworkFailure
 * @throws InvalidArgument
 * @throws Io
 */
export const _lower_prompt = defineFunction("aws.internal._lower_prompt", "sync", ["prompt"]) as (prompt: ai.Prompt, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => BedrockPrompt;

/**
 * @throws InvalidRequest
 * @throws NetworkFailure
 * @throws InvalidArgument
 * @throws Io
 */
export const _lower_prompt_async = defineFunction("aws.internal._lower_prompt", "async", ["prompt"]) as (prompt: ai.Prompt, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<BedrockPrompt>;

export class PendingMessage {
  message!: Message;
  tool_results!: boolean;
  constructor(init: {
    message: Message;
    tool_results: boolean;
  }) {
    Object.assign(this, init);
  }
}

export const _assistant_content = defineFunction("aws.internal._assistant_content", "sync", ["content"]) as (content: ai.content.Block[], $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => ContentBlock[];

export const _assistant_content_async = defineFunction("aws.internal._assistant_content", "async", ["content"]) as (content: ai.content.Block[], $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<ContentBlock[]>;

/**
 * `ToolResultBlock.status` "is only supported by Amazon Nova and Anthropic
 * Claude 3 and 4 models" (API_runtime_ToolResultBlock). Sending it to any
 * other family ships a member that model does not accept, so it is gated on a
 * substring of the model id — which covers a bare id
 * (`anthropic.claude-3-5-…`), a cross-region inference profile
 * (`us.amazon.nova-…`) and a full ARN
 * (`…:foundation-model/anthropic.claude-…`) alike. An unrecognized model (a
 * custom-model ARN, a provisioned-throughput ARN, Mistral/Llama/DeepSeek)
 * omits the field; the result's text still carries the error message, so only
 * the flag is lost.
 */
export const _supports_tool_result_status = defineFunction("aws.internal._supports_tool_result_status", "sync", ["model"]) as (model: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => boolean;

/**
 * `ToolResultBlock.status` "is only supported by Amazon Nova and Anthropic
 * Claude 3 and 4 models" (API_runtime_ToolResultBlock). Sending it to any
 * other family ships a member that model does not accept, so it is gated on a
 * substring of the model id — which covers a bare id
 * (`anthropic.claude-3-5-…`), a cross-region inference profile
 * (`us.amazon.nova-…`) and a full ARN
 * (`…:foundation-model/anthropic.claude-…`) alike. An unrecognized model (a
 * custom-model ARN, a provisioned-throughput ARN, Mistral/Llama/DeepSeek)
 * omits the field; the result's text still carries the error message, so only
 * the flag is lost.
 */
export const _supports_tool_result_status_async = defineFunction("aws.internal._supports_tool_result_status", "async", ["model"]) as (model: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<boolean>;

export const _push_tool_result = defineFunction("aws.internal._push_tool_result", "sync", ["pending", "id", "output", "is_error", "with_status"]) as (pending: PendingMessage[], id: string, output: string, is_error: boolean, with_status: boolean, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => null;

export const _push_tool_result_async = defineFunction("aws.internal._push_tool_result", "async", ["pending", "id", "output", "is_error", "with_status"]) as (pending: PendingMessage[], id: string, output: string, is_error: boolean, with_status: boolean, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<null>;

export const _lower_journal = defineFunction("aws.internal._lower_journal", "sync", ["journal", "with_status"]) as (journal: ai.Journal, with_status: boolean, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Message[];

export const _lower_journal_async = defineFunction("aws.internal._lower_journal", "async", ["journal", "with_status"]) as (journal: ai.Journal, with_status: boolean, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<Message[]>;

export const _tool_choice = defineFunction("aws.internal._tool_choice", "sync", ["choice"]) as (choice: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => ToolChoice;

export const _tool_choice_async = defineFunction("aws.internal._tool_choice", "async", ["choice"]) as (choice: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<ToolChoice>;

/**
 * @throws JsonSerializationError
 */
export const _tool_config = defineFunction("aws.internal._tool_config", "sync", ["toolbox", "tool_choice", "cache_tools"]) as (toolbox: ai.tools.Toolbox, tool_choice: string | null, cache_tools: boolean, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => ToolConfig | null;

/**
 * @throws JsonSerializationError
 */
export const _tool_config_async = defineFunction("aws.internal._tool_config", "async", ["toolbox", "tool_choice", "cache_tools"]) as (toolbox: ai.tools.Toolbox, tool_choice: string | null, cache_tools: boolean, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<ToolConfig | null>;

/**
 * @throws InvalidRequest
 */
export const _inference_config = defineFunction("aws.internal._inference_config", "sync", ["client"]) as (client: vendor.aws.BedrockClient, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => InferenceConfig | null;

/**
 * @throws InvalidRequest
 */
export const _inference_config_async = defineFunction("aws.internal._inference_config", "async", ["client"]) as (client: vendor.aws.BedrockClient, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<InferenceConfig | null>;

export const _system_as_user = defineFunction("aws.internal._system_as_user", "sync", ["system"]) as (system: SystemContentBlock[], $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Message;

export const _system_as_user_async = defineFunction("aws.internal._system_as_user", "async", ["system"]) as (system: SystemContentBlock[], $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<Message>;

export const _merge_adjacent_roles = defineFunction("aws.internal._merge_adjacent_roles", "sync", ["messages"]) as (messages: Message[], $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Message[];

export const _merge_adjacent_roles_async = defineFunction("aws.internal._merge_adjacent_roles", "async", ["messages"]) as (messages: Message[], $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<Message[]>;

/**
 * The typed Converse body for one model turn, before serialization.
 * @throws InvalidRequest
 * @throws NetworkFailure
 * @throws InvalidArgument
 * @throws Io
 * @throws JsonSerializationError
 * @throws CompilationError
 */
export const converse_request = defineFunction("aws.internal.converse_request", "sync", ["client", "input"]) as (client: vendor.aws.BedrockClient, input: ai.ModelTurnInput, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => ConverseRequest;

/**
 * The typed Converse body for one model turn, before serialization.
 * @throws InvalidRequest
 * @throws NetworkFailure
 * @throws InvalidArgument
 * @throws Io
 * @throws JsonSerializationError
 * @throws CompilationError
 */
export const converse_request_async = defineFunction("aws.internal.converse_request", "async", ["client", "input"]) as (client: vendor.aws.BedrockClient, input: ai.ModelTurnInput, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<ConverseRequest>;

/**
 * The Converse endpoint for this client: the `endpoint_url` override when set
 * (which bypasses region resolution entirely), else
 * `https://bedrock-runtime.<region>.amazonaws.com`.
 * @throws InvalidRequest
 * @throws InvalidArgument
 * @throws Io
 * @throws ParseError
 */
export const converse_url = defineFunction("aws.internal.converse_url", "sync", ["client"]) as (client: vendor.aws.BedrockClient, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => string;

/**
 * The Converse endpoint for this client: the `endpoint_url` override when set
 * (which bypasses region resolution entirely), else
 * `https://bedrock-runtime.<region>.amazonaws.com`.
 * @throws InvalidRequest
 * @throws InvalidArgument
 * @throws Io
 * @throws ParseError
 */
export const converse_url_async = defineFunction("aws.internal.converse_url", "async", ["client"]) as (client: vendor.aws.BedrockClient, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<string>;

export const _merge_headers = defineFunction("aws.internal._merge_headers", "sync", ["base", "extra"]) as (base: { [key: string]: string }, extra: { [key: string]: string } | null, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => { [key: string]: string };

export const _merge_headers_async = defineFunction("aws.internal._merge_headers", "async", ["base", "extra"]) as (base: { [key: string]: string }, extra: { [key: string]: string } | null, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<{ [key: string]: string }>;

/**
 * @throws InvalidArgument
 */
export const _apply_query = defineFunction("aws.internal._apply_query", "sync", ["url", "params"]) as (url: string, params: { [key: string]: string } | null, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => string;

/**
 * @throws InvalidArgument
 */
export const _apply_query_async = defineFunction("aws.internal._apply_query", "async", ["url", "params"]) as (url: string, params: { [key: string]: string } | null, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<string>;

/**
 * The complete, UNSIGNED Converse request: typed body -> JSON -> null-pruned
 * -> `request_body` passthrough merged in, plus the user's headers and query
 * parameters. Signing must come after this and nothing may touch the request
 * afterwards, so this is the seam request-shape tests assert on.
 * @throws InvalidRequest
 * @throws NetworkFailure
 * @throws InvalidArgument
 * @throws Io
 * @throws ParseError
 * @throws JsonSerializationError
 * @throws CompilationError
 */
export const build_request = defineFunction("aws.internal.build_request", "sync", ["client", "input"]) as (client: vendor.aws.BedrockClient, input: ai.ModelTurnInput, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => baml.http.Request;

/**
 * The complete, UNSIGNED Converse request: typed body -> JSON -> null-pruned
 * -> `request_body` passthrough merged in, plus the user's headers and query
 * parameters. Signing must come after this and nothing may touch the request
 * afterwards, so this is the seam request-shape tests assert on.
 * @throws InvalidRequest
 * @throws NetworkFailure
 * @throws InvalidArgument
 * @throws Io
 * @throws ParseError
 * @throws JsonSerializationError
 * @throws CompilationError
 */
export const build_request_async = defineFunction("aws.internal.build_request", "async", ["client", "input"]) as (client: vendor.aws.BedrockClient, input: ai.ModelTurnInput, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<baml.http.Request>;

/**
 * SigV4-sign the request. This is the LAST mutation: the signature covers the
 * method, URL, headers and body exactly as built above.
 * @throws InvalidRequest
 */
export const sign = defineFunction("aws.internal.sign", "sync", ["client", "request"]) as (client: vendor.aws.BedrockClient, request: baml.http.Request, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => baml.http.Request;

/**
 * SigV4-sign the request. This is the LAST mutation: the signature covers the
 * method, URL, headers and body exactly as built above.
 * @throws InvalidRequest
 */
export const sign_async = defineFunction("aws.internal.sign", "async", ["client", "request"]) as (client: vendor.aws.BedrockClient, request: baml.http.Request, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<baml.http.Request>;

export class ReasoningText {
  text!: string | null;
  signature!: string | null;
  constructor(init: {
    text: string | null;
    signature: string | null;
  }) {
    Object.assign(this, init);
  }
}

/**
 * Attributes:
 *   reasoningText
 *   redactedContent: Present instead of `reasoningText` when the provider redacted the
 *     thinking; opaque bytes with nothing readable to surface.
 */
export class ReasoningContent {
  reasoningText!: ReasoningText | null;
  redactedContent!: string | null;
  constructor(init: {
    reasoningText: ReasoningText | null;
    redactedContent: string | null;
  }) {
    Object.assign(this, init);
  }
}

export class ResponseToolUse {
  toolUseId!: string | null;
  name!: string | null;
  input!: { [key: string]: unknown } | null;
  constructor(init: {
    toolUseId: string | null;
    name: string | null;
    input: { [key: string]: unknown } | null;
  }) {
    Object.assign(this, init);
  }
}

export class ResponseContentBlock {
  text!: string | null;
  toolUse!: ResponseToolUse | null;
  reasoningContent!: ReasoningContent | null;
  constructor(init: {
    text: string | null;
    toolUse: ResponseToolUse | null;
    reasoningContent: ReasoningContent | null;
  }) {
    Object.assign(this, init);
  }
}

export class ResponseMessage {
  role!: string | null;
  content!: ResponseContentBlock[] | null;
  constructor(init: {
    role: string | null;
    content: ResponseContentBlock[] | null;
  }) {
    Object.assign(this, init);
  }
}

export class ConverseOutput {
  message!: ResponseMessage | null;
  constructor(init: {
    message: ResponseMessage | null;
  }) {
    Object.assign(this, init);
  }
}

/**
 * Converse token accounting. `cacheReadInputTokens` / `cacheWriteInputTokens`
 * are reported for prompt-cached requests — sys_llm hardcoded
 * `cached_input_tokens: None` claiming Converse never reports them
 * (`parse_response/bedrock.rs:124`), which was simply wrong; the engine read
 * them (`aws_client.rs:1481-1484`). Fixed here.
 */
export class ConverseUsage {
  inputTokens!: number | null;
  outputTokens!: number | null;
  totalTokens!: number | null;
  cacheReadInputTokens!: number | null;
  cacheWriteInputTokens!: number | null;
  constructor(init: {
    inputTokens: number | null;
    outputTokens: number | null;
    totalTokens: number | null;
    cacheReadInputTokens: number | null;
    cacheWriteInputTokens: number | null;
  }) {
    Object.assign(this, init);
  }
}

export class ConverseMetrics {
  latencyMs!: number | null;
  constructor(init: {
    latencyMs: number | null;
  }) {
    Object.assign(this, init);
  }
}

export class ConverseResponse {
  output!: ConverseOutput | null;
  stopReason!: string | null;
  usage!: ConverseUsage | null;
  metrics!: ConverseMetrics | null;
  additionalModelResponseFields!: baml.json.json | null;
  constructor(init: {
    output: ConverseOutput | null;
    stopReason: string | null;
    usage: ConverseUsage | null;
    metrics: ConverseMetrics | null;
    additionalModelResponseFields: baml.json.json | null;
  }) {
    Object.assign(this, init);
  }
}

/**
 * True for the two `stopReason` values that say the turn's own content is
 * unusable: `malformed_model_output` (the model emitted something the service
 * could not assemble into a message) and `malformed_tool_use` (it emitted a
 * tool call the service could not assemble into a `toolUse` block).
 * 
 * Neither is a stop *reason* in the `ai.content.StopReason` sense — there is
 * no answer to stop on — so `parse_response` throws `ai.errors.ParseFailed`
 * with the whole response as `raw_output` instead of returning a turn. That
 * is the honest identity (it IS a failure to produce parseable output), it is
 * `RetrySafety.Safe` so a Retry policy re-asks, and it keeps the raw bytes
 * for the caller. `MaxTokens` would falsely blame the token limit and
 * `Refused` would falsely claim the model declined.
 */
export const _is_malformed_stop = defineFunction("aws.internal._is_malformed_stop", "sync", ["raw"]) as (raw: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => boolean;

/**
 * True for the two `stopReason` values that say the turn's own content is
 * unusable: `malformed_model_output` (the model emitted something the service
 * could not assemble into a message) and `malformed_tool_use` (it emitted a
 * tool call the service could not assemble into a `toolUse` block).
 * 
 * Neither is a stop *reason* in the `ai.content.StopReason` sense — there is
 * no answer to stop on — so `parse_response` throws `ai.errors.ParseFailed`
 * with the whole response as `raw_output` instead of returning a turn. That
 * is the honest identity (it IS a failure to produce parseable output), it is
 * `RetrySafety.Safe` so a Retry policy re-asks, and it keeps the raw bytes
 * for the caller. `MaxTokens` would falsely blame the token limit and
 * `Refused` would falsely claim the model declined.
 */
export const _is_malformed_stop_async = defineFunction("aws.internal._is_malformed_stop", "async", ["raw"]) as (raw: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<boolean>;

/**
 * One wire `stopReason` -> the canonical stop reason
 * (`parse_response/bedrock.rs:110-116`, extended with the guardrail and
 * failure reasons the Converse API documents).
 */
export const stop_reason = defineFunction("aws.internal.stop_reason", "sync", ["raw"]) as (raw: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => ai.content.StopReason;

/**
 * One wire `stopReason` -> the canonical stop reason
 * (`parse_response/bedrock.rs:110-116`, extended with the guardrail and
 * failure reasons the Converse API documents).
 */
export const stop_reason_async = defineFunction("aws.internal.stop_reason", "async", ["raw"]) as (raw: string, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<ai.content.StopReason>;

/**
 * Decode one Converse response into canonical content blocks.
 * 
 * Unlike sys_llm (`parse_response/bedrock.rs:74-104`) this does NOT error on
 * an empty content list or on a turn with no text blocks: a tool-only turn is
 * the normal `stopReason: "tool_use"` shape and erroring on it would make
 * tool calling impossible. A missing `output.message` is still a hard error —
 * that response is malformed, not empty.
 * @throws ParseFailed
 * @throws JsonSerializationError
 */
export const parse_response = defineFunction("aws.internal.parse_response", "sync", ["response"]) as (response: ConverseResponse, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => ai.ModelTurn;

/**
 * Decode one Converse response into canonical content blocks.
 * 
 * Unlike sys_llm (`parse_response/bedrock.rs:74-104`) this does NOT error on
 * an empty content list or on a turn with no text blocks: a tool-only turn is
 * the normal `stopReason: "tool_use"` shape and erroring on it would make
 * tool calling impossible. A missing `output.message` is still a hard error —
 * that response is malformed, not empty.
 * @throws ParseFailed
 * @throws JsonSerializationError
 */
export const parse_response_async = defineFunction("aws.internal.parse_response", "async", ["response"]) as (response: ConverseResponse, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<ai.ModelTurn>;

/**
 * @throws InvalidArgument
 * @throws Io
 * @throws ParseError
 * @throws JsonSerializationError
 * @throws CompilationError
 */
export const invoke = defineFunction("aws.internal.invoke", "sync", ["client", "input"]) as (client: vendor.aws.BedrockClient, input: ai.ModelTurnInput, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => ai.ModelTurn;

/**
 * @throws InvalidArgument
 * @throws Io
 * @throws ParseError
 * @throws JsonSerializationError
 * @throws CompilationError
 */
export const invoke_async = defineFunction("aws.internal.invoke", "async", ["client", "input"]) as (client: vendor.aws.BedrockClient, input: ai.ModelTurnInput, $opts?: { $ctx?: BamlCallContext | undefined } | undefined) => Promise<ai.ModelTurn>;
